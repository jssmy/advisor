import { OnClickElementDirective } from './on-click-element.directive';

describe('OnClickElementDirective', () => {
  it('should create an instance', () => {
    const directive = new OnClickElementDirective();
    expect(directive).toBeTruthy();
  });
});
